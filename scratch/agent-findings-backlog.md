# Diagnosed fix backlog (from agent investigations, 2026-06-10)

Each item: tests flipped, root cause location, fix sketch, risk. Full reports in session transcript; probes in scratch/.

## Quick wins (low risk)
- [ ] B4 eval class completion (1 test: statements/class/cptn-decl.js): emission/eval/programs.rs:1798-1839 resolve_static_eval_statement_completion_expression — `Statement::Declaration => None` (declarations have empty completion); currently recurses into lowered class body and returns a defineProperty handle.
- [ ] A5 restricted caller/arguments with own static accessor (1: definition/setters-restricted-ids.js): calls/user_calls/resolution/binding_resolution.rs:330-354 is_restricted_function_property — return false when object has statically-known own setter of that name (consult resolve_member_setter_binding).
- [ ] A7 class name re-assert ordering (1: definition/fn-name-static-precedence.js): frontend/lowering/classes.rs:324-333 class_prop_name_is_name only sees literal keys; trailing defineProperty(C,'name') re-emitted AFTER static members clobbers computed `static [attr]()`. Emit name define only before element evaluation.
- [ ] A6.1 bound-snapshot Print bail (2+: definition/methods-async-super-call-{body,param}.js partially): calls/call_resolution/bound_snapshots/statements.rs:390-400 evaluates Statement::Print only for throw effects then DISCARDS output; make snapshot bail (None) on Print or unresolvable await operands (esp. await super.method()). Runtime half (promise chain never settles) needs separate work.

## Medium clusters
- [ ] B2 speculative no_change suppresses runtime store (7 cpn fields tests; statics only — instance fields now work): descriptor_calls.rs:1227-1238. REFINED (2026-06-10): the defineProperty's own key is already static (hoisted to __ayy_class_prop_N), so gating on key-effectfulness won't help. The poisoning is cross-pass: pass-1 applies the define to the global object binding for __ayy_class_expr_N; pass-2's static_property_current_descriptor sees it → no_change=true → skips the runtime store, and the READ C[x|=1] has an effectful key → dynamic lookup → miss. Most defines avoid this because they fail target/descriptor static resolution. Fix needs either pass-aware state (compare against pre-statement snapshot / reset class-expr bindings before final pass) or: when the no_change conclusion comes from a target whose properties are not all materialized in the runtime dynamic table, fall through (return None) instead. Caller semantics: Some(x) skips real emission (descriptor_calls.rs:2938-2948); None falls through to real emission.
- [ ] B1 String(fn)/String(obj) sentinel (9+ cpn tests + generator-function-declaration variants): calls/builtin_calls/dispatch.rs:857-876 — String(x) unresolvable → JS_TYPEOF_STRING_TAG sentinel (length undefined). Route String(fn) through Function.prototype.toString source-text mechanism; implement String(obj) → "[object Tag]" reusing tag machinery at emission/eval/call_entry.rs:902. Key coercion (emission/assignments/property_keys/coercion.rs) must agree.
- [ ] A2 function own-property introspection (4: definition/prototype-property, fn-name-method, fn-name-gen-method, fn-name-static-precedence-order): descriptor_calls.rs:~2090 resolve_descriptor_binding_from_expression — synthesize descriptors for fn `prototype` (w:false,e:false,c:false for class; w:true for ordinary fns!), `name`/`length` (w:false,e:false,c:true); add function support to getOwnPropertyNames (length, name, prototype, statics).
- [ ] A4 class heritage validation (4: definition/constructable-but-no-prototype, invalid-extends, prototype-getter, prototype-setter): descriptor_calls.rs:1927 emit_class_prototype_init_call — no IsConstructor check (extends 42 must TypeError), no Get(heritage,'prototype') type check, bound functions must lack own prototype.

## Deep / systemic (from call-boundary agent)
- [ ] Parameter kind union (cosmetic but broad): program_compiler/globals/parameters/value_bindings/call_registration.rs:237-256 merge_parameter_value_binding_candidate — conflicting call sites → binding None, no kind retained; keep per-parameter kind when all sites share infer_value_kind (String!), thread into binding_seed.rs:233. Fixes strings printing as raw handles through calls.
- [ ] Deleted-marker hygiene: runtime_shadows.rs:2921-2924 eagerly creates deleted-shadow markers for every synced property; :2347-2361 treats existence as may-affect. Require marker to be assigned by a real delete. Restores kind/value for untouched object props (e.g. console.log("direct", o.msg) printing handle).
- [ ] Ctor effects through super() (flips class/arguments/access.js + default-constructor.js): calls/user_calls/analysis/call_effects/expression_traversal.rs:125-151 records frontend temps (__ayy_target_object_N) as effect targets — resolve alias to real owner; captures.rs:1234-1252 derived ctors updated=None → conservatively collect base-ctor effects via super() and invalidate; ensure shadow globals' initial data isn't final-static-state.
- [ ] SuppressedError property identity (1: await-using/throws-suppressederror-...): frontend/lowering/statements.rs:360-373,399-411 — `error` property is a LIVE alias Identifier(__ayy_using_dispose_error_N); capture by value per instance, or block member_reads.rs:408-419 alias substitution for multiply-assigned temps crossing async boundaries.
- [ ] A1 setter receiver write-back stale fallback (definition/accessors.js + likely many): snapshot_this.rs:3810-3878 finalize_user_function_runtime_this_shadow_state uses PRE-CALL receiver binding for shadow copy fallbacks; compute from post-sync binding. Probes: clsdef-probe-setter-b3/b5.
- [ ] A3 __ayy_prototype_snapshot_* unresolvable (definition/side-effects-in-extends, numeric-property-names): binding_sync.rs:773-806 creates synthetic owners with no runtime binding (ReferenceError on Object.getPrototypeOf(D.prototype)) and super resolution can't see them (super_properties.rs:308-341). Add identifier-read fallback mirroring __ayy_class_super_ at identifier_reads.rs:291-303.
- [ ] B3 TLA + try/catch drops continuation (3 await-expression cpn tests): module top-level-await state machine exits cleanly without post-try code. Repro pair: scratch/clsdef-probe-t262/.../clsdef-probe-await-min8.js (fails) vs -minB.js (passes, no try).
- [ ] A8 derived-ctor this TDZ + base-return substitution (1: this-access-restriction-2.js): high risk, classes.rs ~856-931 + ctor-return handling. Lowest priority.

## swc vendored parser (vendor/swc_ecma_parser, [patch.crates-io])
- [x] token.rs is_reserved: await reserved in Module (not Strict scripts) — fixes class-name-ident-await-escaped ×2.
- [x] class_and_fn.rs parse_decorator: context-sensitive parse_ident — fixes @await decorators ×6.
- [ ] await-label (`await: 1;`, 2 tests statements/labeled/value-await-non-module{,-escaped}): stmt.rs guard added (await+colon skips await-stmt arm) but expression path still TS1109 — the EXPRESSION parser's await handling needs the same label-awareness (parse_await_expr sees `:` and errors). Find where expression-statement parses Await token.

## Perf (profiling agent report landed 2026-06-10; worktree .claude/worktrees/agent-ac088919313b00f8f preserved with instrumentation patch + A/B binaries)
- [x] SHIPPED (1.8x): leaf fast-path in materialize_static_expression + ayy_env_flag! OnceLock caching of 481 AYY_* trace-guard getenv sites.
- [ ] Memoize call/binding resolution per (expression shape, static-state GENERATION COUNTER) — bump counter in mutation funnels (set_local_*, sync_global_object_binding, update_static_global_assignment_metadata, metadata_rollback.rs, structured_control.rs:909-939); cache resolve_static_call_result_expression_with_context / resolve_call_or_new_object_binding / resolve_function_binding_from_expression_with_context. 162M→~10^4 resolutions; est. 10-100x. MEDIUM RISK: missed funnel = stale result = miscompile; add debug-assert compare mode.
- [ ] Static-loop fixpoint early-out (block.rs While/For): if an iteration leaves environment+iterator state unchanged and condition re-evaluates identically → return None immediately instead of 4096 iterations. Plus negative-cache failed generator-tail static executions (key: simple_generator_source_cache_key + step index). Est. ~1000x on generator-driver shapes (cpn-yield 10+min, tco-call-args).
- [ ] Replace format!("{expr:?}") guard keys (user_calls/resolution/guard.rs:50, static_call_results.rs:25, source_kind.rs:288) + linear static_expression_matches stack scan in with_object_binding_resolution_guard (core_resolution.rs:15-28) with structural hash. ~40-60% of cycles per sampler; est. 1.5-2x more.
- [ ] simple_generator_source_environment_cache_key Debug-formats + sorts entire environment per LOOKUP (source_resolution.rs:4) — replace with generation counter. Landmine, cold in profiled workloads.
- Structure of the blowup: ~15-strategy resolve_call_object_binding fallback chain, each strategy re-resolving the same sub-chain from scratch, (strategies)^depth × O(expr-size); plus 4096-iteration static while-loops re-executing unresolvable generator tails (~21ms each, never negatively cached), × 3-5 pre-passes.

## Notes
- forbidden-ext: ~140 other unchecked forbidden-ext tests now mostly PASS per spot-checks (stale tracker) — wave probes will catch them.
- Bonus bugs found: "str " + typeof m miscompiles to NaN (m from this.method); compiler stack overflow on 3 consecutive asyncMethod().then(arrow) statements (scratch/dfx-probe-fext3.js).

## class/dstr cluster (agent af1d754611f37978b, 2026-06-10; probes scratch/clsdstr-probe-*)
30 confirmed failures in statements/class/dstr (private-meth framing was sampling artifact; plain meth fail identically). Priority C→B→A→D.
- [ ] C obj rest with accessor (~30-38 flips): PARTIAL 2026-06-10 — the deleted-marker emitted-set fix moved x.v reads from deleted_shadow_hit to binding_hit, but the binding still lacks 'v': resolve_object_binding_entries_with_state (stateful_resolution.rs:851+) computes the getter value during INLINE analysis in a discarded environment; body emission's stateless resolve_object_binding_from_expression never sees it. Remaining fix: commit the resolved spread binding into static semantics keyed by the inline param name (where inline param bindings are seeded) so stateless resolution finds it.
- [ ] B rest elision identity (~12-16 flips, LOW-MED): `[9,9] === {a:1}` → true through params (placeholder tag compare, binary.rs ~:3012). Mint identity keys for fresh allocations (__ayy_array_rest_*; expression_is_fresh_object_allocation comparisons.rs:22 exists; fresh-alloc branch :223-227 answers Some(false)); narrowest: don't bail resolve_static_object_identity_expression on prototype resolution for tracked rest arrays (object_identity.rs:619-624).
- [ ] A iterator-protocol via method params (~70-100 flips, MED): custom-iterable args don't propagate object binding into method-body emission; emit_get_iterator_expression fallback (iterators.rs:856-859) emits placeholder. Fix in source_kind.rs:436-486: when value chain ends at __ayy_param_*, resolve call-site argument expr and re-run resolve_iterator_source_kind.
- [ ] D static-iterable enumeration explosion (timeouts; LOW): static_iterables.rs:824 enumerates 256 steps per query, unmemoized (77K queries/15-line file). Memoize per (source-expr, context); cap by destructuring arity.
- [ ] BONUS: runtime string concat fallback adds interned tag numerically ("len="+x.length → 778); emit_runtime_string_addition_from_candidates binary.rs:1903-1928, numeric fallback :3012.

## Session notes (2026-06-10, post-restore)
- compound-assignment agent branch worktree-agent-ab89dcaa51b3c9b68 (commits f7309ba, 7277108) delivered 198/277 cluster flips BUT was based on stale pre-memoization commit 22e3918; cherry-pick onto main conflicts in 5 files (capture_state.rs, immediate_promises.rs, async_next.rs, static_resolution.rs, call_registration.rs region). Needs a rebase-and-regate round (incl. AYY_MEMO_VERIFY since its snapshot-store paths predate the generation counter). Remaining 79 cluster failures: getter-with-delete-this.x PutValue write-through family (~75, see its report: scoped_properties.rs + getter_calls.rs read/write-back ordering) + 4 bigint.

## Round status (2026-06-11, ~91.4%)
- [x] yield-star compile pathology #1 SHIPPED (emit_prepare capture re-entrancy guard + literal early-outs + name-only scans): specimens 150s-SO → <9s; 19 yield-star tests now compile (runtime-fail, awaiting async-gen runtime fixes). Pathology #2 residual: ~17 timeouts in private-method-STATIC/-async-next family — blunt serial-0 note_resolution_guard_block() sites (~20) poison memo windows; convert to serial-aware conflict notes (architectural follow-up).
- for-of dstr: 8 verified flips marked; agent-measured extras didn't reproduce on merged main — re-attack alongside async-step round.
- IN FLIGHT: async-step fidelity resume agent (worktree agent-a5ac87953fa632c7b, inherited +99/-6 diff).
